{
  "name": "yamshi-pixels-shopify",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "YAMSHI Pixels — unlimited Meta pixels + Conversion API for Shopify",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@shopify/shopify-api": "^11.12.0",
    "compression": "^1.8.0",
    "dotenv": "^16.5.0",
    "express": "^4.21.2"
  }
}
